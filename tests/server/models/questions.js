import {expect} from 'chai';
import chaiHttp from 'chai-http';

import DB from '../../../server/db';
import QuestionModel from '../../../server/models/questions';

describe('Question Model', () => {
  
  it('can create a new question', () => {
    // query DB
    // run user Create func

  });
  it('can get all questions', () => {
    // query DB
    //  

  });
  it('can remove a question', () => {
    // query DB
    //  

  });
});