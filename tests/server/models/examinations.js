import {expect} from 'chai';
import chaiHttp from 'chai-http';

import DB from '../../../server/db';
import ExaminationModel from '../../../server/models/examinations';

describe('Examination Model', () => {
  
  it('can create a new examination', () => {
    // query DB
    // run user Create func

  });
  it('can get all examinations', () => {
    // query DB
    //  

  });
  it('can remove a examination', () => {
    // query DB
    //  

  });
});